-- Script SQL pour créer la base de données feedback_db
-- Exécuter cette commande dans psql ou pgAdmin

-- Créer la base de données
CREATE DATABASE feedback_db;

-- Optionnel : Vérifier que la base a été créée
-- SELECT datname FROM pg_database WHERE datname = 'feedback_db';

